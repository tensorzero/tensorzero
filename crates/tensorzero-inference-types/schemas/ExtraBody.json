{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ExtraBody",
  "anyOf": [
    {
      "title": "VariantExtraBody",
      "type": "object",
      "properties": {
        "variant_name": {
          "description": "A variant name in your configuration (e.g. `my_variant`)",
          "type": "string"
        },
        "pointer": {
          "description": "A JSON Pointer to the field to update (e.g. `/enable_agi`)",
          "type": "string"
        },
        "value": {
          "description": "The value to set the field to"
        }
      },
      "additionalProperties": false,
      "required": [
        "variant_name",
        "pointer",
        "value"
      ]
    },
    {
      "title": "VariantExtraBodyDelete",
      "type": "object",
      "properties": {
        "variant_name": {
          "description": "A variant name in your configuration (e.g. `my_variant`)",
          "type": "string"
        },
        "pointer": {
          "description": "A JSON Pointer to the field to update (e.g. `/enable_agi`)",
          "type": "string"
        },
        "delete": {
          "description": "Set to true to remove the field from the model provider request's body",
          "type": "boolean",
          "const": true
        }
      },
      "additionalProperties": false,
      "required": [
        "variant_name",
        "pointer",
        "delete"
      ]
    },
    {
      "title": "ModelProviderExtraBody",
      "type": "object",
      "properties": {
        "model_name": {
          "description": "A model name in your configuration (e.g. `my_gpt_5`) or a short-hand model name (e.g. `openai::gpt-5`)",
          "type": "string"
        },
        "provider_name": {
          "description": "A provider name for the model you specified (e.g. `my_openai`)",
          "type": [
            "string",
            "null"
          ]
        },
        "pointer": {
          "description": "A JSON Pointer to the field to update (e.g. `/enable_agi`)",
          "type": "string"
        },
        "value": {
          "description": "The value to set the field to"
        }
      },
      "additionalProperties": false,
      "required": [
        "model_name",
        "pointer",
        "value"
      ]
    },
    {
      "title": "ModelProviderExtraBodyDelete",
      "type": "object",
      "properties": {
        "model_name": {
          "description": "A model name in your configuration (e.g. `my_gpt_5`) or a short-hand model name (e.g. `openai::gpt-5`)",
          "type": "string"
        },
        "provider_name": {
          "description": "A provider name for the model you specified (e.g. `my_openai`)",
          "type": [
            "string",
            "null"
          ]
        },
        "pointer": {
          "description": "A JSON Pointer to the field to update (e.g. `/enable_agi`)",
          "type": "string"
        },
        "delete": {
          "description": "Set to true to remove the field from the model provider request's body",
          "type": "boolean",
          "const": true
        }
      },
      "additionalProperties": false,
      "required": [
        "model_name",
        "pointer",
        "delete"
      ]
    },
    {
      "title": "AlwaysExtraBody",
      "type": "object",
      "properties": {
        "pointer": {
          "description": "A JSON Pointer to the field to update (e.g. `/enable_agi`)",
          "type": "string"
        },
        "value": {
          "description": "The value to set the field to"
        }
      },
      "additionalProperties": false,
      "required": [
        "pointer",
        "value"
      ]
    },
    {
      "title": "AlwaysExtraBodyDelete",
      "type": "object",
      "properties": {
        "pointer": {
          "description": "A JSON Pointer to the field to update (e.g. `/enable_agi`)",
          "type": "string"
        },
        "delete": {
          "description": "Set to true to remove the field from the model provider request's body",
          "type": "boolean",
          "const": true
        }
      },
      "additionalProperties": false,
      "required": [
        "pointer",
        "delete"
      ]
    }
  ]
}
