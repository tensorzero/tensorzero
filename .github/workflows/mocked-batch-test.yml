name: Mocked Batch Tests (for Merge Queue)

# Fake credentials - gateway validates presence but doesn't use them in mock mode
env:
  CARGO_INCREMENTAL: 0
  ANTHROPIC_API_KEY: "fake_anthropic_key"
  AWS_ACCESS_KEY_ID: "fake_aws_access_key"
  AWS_BEARER_TOKEN_BEDROCK: ${{ secrets.AWS_BEARER_TOKEN_BEDROCK }}
  AWS_REGION: "us-east-1"
  AWS_SECRET_ACCESS_KEY: "fake_aws_secret_key"
  AZURE_API_KEY: "fake_azure_openai_key"
  DEEPSEEK_API_KEY: "fake_deepseek_key"
  FIREWORKS_API_KEY: "fake_fireworks_key"
  FORCE_COLOR: 1
  GCP_VERTEX_CREDENTIALS_PATH: ${{ github.workspace }}/gcp_jwt_key.json
  GOOGLE_APPLICATION_CREDENTIALS: ${{ github.workspace }}/gcp_jwt_key.json
  GOOGLE_AI_STUDIO_API_KEY: "fake_google_ai_studio_key"
  GROQ_API_KEY: "fake_groq_key"
  HYPERBOLIC_API_KEY: "fake_hyperbolic_key"
  MISTRAL_API_KEY: "fake_mistral_key"
  OPENAI_API_KEY: "fake_openai_key"
  OPENROUTER_API_KEY: "fake_openrouter_key"
  SGLANG_API_KEY: "fake_sglang_key"
  TGI_API_KEY: "fake_tgi_key"
  TOGETHER_API_KEY: "fake_together_key"
  VLLM_API_KEY: "fake_vllm_key"
  VLLM_API_BASE: "http://fake-vllm-endpoint:8000"
  VLLM_MODEL_NAME: "microsoft/Phi-3.5-mini-instruct"
  XAI_API_KEY: "fake_xai_key"
  TENSORZERO_INTERNAL_MOCK_PROVIDER_API: http://localhost:3030

on:
  workflow_dispatch:
  workflow_call:
    secrets:
      AWS_BEARER_TOKEN_BEDROCK:
        required: true
      DOCKERHUB_LIMITED_TOKEN:
        required: true
      GCP_JWT_KEY:
        required: true

permissions:
  contents: read # Required for checkout or repository metadata reads.
  actions: read # Required to read workflow run, cache, or artifact metadata.

concurrency:
  group: mocked-batch-test-${{ github.event.pull_request.number || github.ref || github.run_id }}
  cancel-in-progress: false

jobs:
  mocked-batch-tests:
    name: mocked-batch-tests
    runs-on: ubuntu-latest
    if: github.repository == 'tensorzero/tensorzero'

    timeout-minutes: 30

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Cleanup disk space
        run: bash ci/free-disk-space.sh

      - name: Install Rust (from rust-toolchain.toml)
        working-directory: crates
        run: |
          for attempt in 1 2 3; do
            if timeout 5m rustup show; then
              break
            fi
            if [ $attempt -eq 3 ]; then
              echo "Failed to install Rust toolchain after 3 attempts"
              exit 1
            fi
            sleep $((10 * attempt))
          done
        shell: bash

      - name: Login to Docker Hub
        uses: docker/login-action@74a5d142397b4f367a81961eba4e8cd7edddf772 # v3.4.0
        with:
          username: tensorzero
          password: ${{ secrets.DOCKERHUB_LIMITED_TOKEN }}

      - name: Install cargo-nextest
        uses: taiki-e/install-action@60581cd7025e0e855cebd745379013e286d9c787 # v2.64.2
        with:
          tool: cargo-nextest

      - name: Write GCP JWT key to file (for GCS access)
        env:
          GCP_JWT_KEY: ${{ secrets.GCP_JWT_KEY }}
        run: echo "$GCP_JWT_KEY" > $GITHUB_WORKSPACE/gcp_jwt_key.json

      - name: Pull mock-provider-api container
        id: pull-mock-provider-api
        continue-on-error: true # Skip if missing (e.g. triggered via `workflow_dispatch`)
        run: |
          ./ci/docker-pull-with-retry.sh \
            tensorzero/mock-provider-api:sha-$GITHUB_SHA

      - name: Pull gateway container
        run: |
          ./ci/docker-pull-with-retry.sh \
            tensorzero/gateway-dev:sha-$GITHUB_SHA
          docker tag tensorzero/gateway-dev:sha-$GITHUB_SHA tensorzero/gateway:sha-$GITHUB_SHA

      - name: Set TENSORZERO_MOCK_PROVIDER_API_TAG
        if: steps.pull-mock-provider-api.outcome == 'success' # Only run if pull was successful
        run: |
          echo "TENSORZERO_MOCK_PROVIDER_API_TAG=sha-$GITHUB_SHA" >> $GITHUB_ENV

      # This is for postgres migrations to run the correct gateway
      - name: Set container image tags
        run: |
          echo "TENSORZERO_GATEWAY_TAG=sha-$GITHUB_SHA" >> $GITHUB_ENV
          echo "TENSORZERO_COMMIT_TAG=sha-$GITHUB_SHA" >> $GITHUB_ENV

      - name: Start Docker services (mock-provider-api + dependencies)
        run: |
          docker compose -f crates/tensorzero-core/tests/e2e/docker-compose.yml up --no-build -d --wait clickhouse postgres gateway-clickhouse-migrations gateway-postgres-migrations minio mock-provider-api

      - name: Set up TENSORZERO_CLICKHOUSE_URL for batch tests
        run: |
          echo "TENSORZERO_CLICKHOUSE_URL=http://chuser:chpassword@localhost:8123/tensorzero_e2e_tests" >> $GITHUB_ENV

      - name: Launch the gateway for batch tests
        working-directory: crates
        run: |
          cargo run-e2e > batch_logs.txt 2>&1 &
          echo "GATEWAY_PID=$!" >> $GITHUB_ENV
          while ! curl -s -f http://localhost:3000/health >/dev/null 2>&1; do
            echo "Waiting for gateway to be healthy..."
            sleep 1
          done

      - name: Run mocked batch tests
        working-directory: crates
        run: |
          cargo test-mock-batch --no-fail-fast

      - name: Print docker compose logs
        if: always()
        run: |
          docker compose -f crates/tensorzero-core/tests/e2e/docker-compose.yml logs -t

      - name: Print batch logs
        if: always()
        working-directory: crates
        run: cat batch_logs.txt
