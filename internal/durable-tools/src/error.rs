use durable::{ControlFlow, DurableError, TaskError};
use serde::Serialize;
use serde_json::Value as JsonValue;
use thiserror::Error;

/// Error type for tool execution.
///
/// This enum separates control flow signals (which are internal and not serializable)
/// from actual errors (which can be serialized for API responses).
#[derive(Debug, Error)]
pub enum ToolError {
    /// Durable control flow signal (suspend, cancelled).
    /// This is internal and should never be serialized.
    #[error("Control flow: {0:?}")]
    Control(ControlFlow),

    /// A serializable tool error.
    #[error(transparent)]
    Error(#[from] SerializableToolError),
}

/// Serializable error type for tool execution.
///
/// This enum contains framework-level errors generated by durable-tools itself,
/// plus a `User` variant for tool-specific errors. Tool implementations should
/// define their own error types and convert them to `SerializableToolError::User`.
#[derive(Debug, Error, Serialize)]
#[serde(tag = "kind", rename_all = "snake_case")]
pub enum SerializableToolError {
    /// Tool was not found in the registry.
    #[error("Tool not found: {name}")]
    ToolNotFound { name: String },

    /// Tool with this name is already registered.
    #[error("Tool '{name}' is already registered. Each tool name must be unique.")]
    DuplicateToolName { name: String },

    /// Parameter validation or parsing failed.
    #[error("Parameter error: {message}")]
    InvalidParams { message: String },

    /// Schema generation failed.
    #[error("Schema generation failed: {message}")]
    SchemaGeneration {
        message: String,
        #[source]
        #[serde(skip)]
        source: anyhow::Error,
    },

    /// JSON serialization/deserialization error.
    #[error("Serialization error: {message}")]
    Serialization {
        message: String,
        #[source]
        #[serde(skip)]
        source: serde_json::Error,
    },

    /// Database operation failed.
    #[error("Database error: {message}")]
    Database {
        message: String,
        #[source]
        #[serde(skip)]
        source: sqlx::Error,
    },

    /// Operation timed out.
    #[error("Timed out waiting for '{step_name}'")]
    Timeout { step_name: String },

    /// Validation error.
    #[error("Validation error: {message}")]
    Validation { message: String },

    /// Internal task error.
    #[error("Internal error: {message}")]
    Internal { message: String },

    /// Child task failed.
    #[error("Child task failed at '{step_name}': {message}")]
    ChildFailed { step_name: String, message: String },

    /// Child task was cancelled.
    #[error("Child task was cancelled at '{step_name}'")]
    ChildCancelled { step_name: String },

    /// Invalid durable configuration.
    #[error("Invalid configuration: {reason}")]
    InvalidConfiguration { reason: String },

    /// Reserved header prefix used.
    #[error("Header key '{key}' uses reserved prefix 'durable::'")]
    ReservedHeaderPrefix { key: String },

    /// Invalid event name.
    #[error("Invalid event name: {reason}")]
    InvalidEventName { reason: String },

    /// User-defined error from tool implementation.
    ///
    /// Tool implementations should define their own error types and convert
    /// them to this variant. The `error_data` field stores structured JSON
    /// that can be deserialized back to the original error type.
    #[error("{message}")]
    User {
        /// Human-readable error message.
        message: String,
        /// Structured error data for persistence and later retrieval.
        error_data: JsonValue,
    },
}

/// Result type alias for tool operations.
pub type ToolResult<T> = Result<T, ToolError>;

impl From<serde_json::Error> for ToolError {
    fn from(e: serde_json::Error) -> Self {
        ToolError::Error(SerializableToolError::Serialization {
            message: e.to_string(),
            source: e,
        })
    }
}

impl From<sqlx::Error> for ToolError {
    fn from(e: sqlx::Error) -> Self {
        ToolError::Error(SerializableToolError::Database {
            message: e.to_string(),
            source: e,
        })
    }
}

impl From<TaskError> for ToolError {
    fn from(err: TaskError) -> Self {
        match err {
            TaskError::Control(cf) => ToolError::Control(cf),
            TaskError::Database(e) => ToolError::Error(SerializableToolError::Database {
                message: e.to_string(),
                source: e,
            }),
            TaskError::Serialization(e) => ToolError::Error(SerializableToolError::Serialization {
                message: e.to_string(),
                source: e,
            }),
            TaskError::Timeout { step_name } => {
                ToolError::Error(SerializableToolError::Timeout { step_name })
            }
            TaskError::User {
                message,
                error_data,
            } => ToolError::Error(SerializableToolError::User {
                message,
                error_data,
            }),
            TaskError::Validation { message } => {
                ToolError::Error(SerializableToolError::Validation { message })
            }
            TaskError::TaskInternal(e) => ToolError::Error(SerializableToolError::Internal {
                message: e.to_string(),
            }),
            TaskError::ChildFailed { step_name, message } => {
                ToolError::Error(SerializableToolError::ChildFailed { step_name, message })
            }
            TaskError::ChildCancelled { step_name } => {
                ToolError::Error(SerializableToolError::ChildCancelled { step_name })
            }
        }
    }
}

impl From<ToolError> for TaskError {
    fn from(err: ToolError) -> Self {
        match err {
            ToolError::Control(cf) => TaskError::Control(cf),
            ToolError::Error(e) => e.into(),
        }
    }
}

impl From<SerializableToolError> for TaskError {
    fn from(err: SerializableToolError) -> Self {
        match err {
            SerializableToolError::Database { source, .. } => TaskError::Database(source),
            SerializableToolError::Serialization { source, .. } => TaskError::Serialization(source),
            SerializableToolError::Timeout { step_name } => TaskError::Timeout { step_name },
            SerializableToolError::User {
                message,
                error_data,
            } => TaskError::User {
                message,
                error_data,
            },
            SerializableToolError::Validation { message } => TaskError::Validation { message },
            SerializableToolError::Internal { message } => {
                TaskError::TaskInternal(anyhow::anyhow!("{message}"))
            }
            SerializableToolError::ChildFailed { step_name, message } => {
                TaskError::ChildFailed { step_name, message }
            }
            SerializableToolError::ChildCancelled { step_name } => {
                TaskError::ChildCancelled { step_name }
            }
            // For all other variants, use the Serialize impl to generate error_data
            other => {
                let error_data = serde_json::to_value(&other).unwrap_or_else(|e| {
                    serde_json::json!({
                        "kind": "error_serialization_failed",
                        "serialization_error": e.to_string(),
                    })
                });
                let message = other.to_string();
                TaskError::User {
                    message,
                    error_data,
                }
            }
        }
    }
}

impl From<DurableError> for ToolError {
    fn from(err: DurableError) -> Self {
        match err {
            DurableError::Database(e) => ToolError::Error(SerializableToolError::Database {
                message: e.to_string(),
                source: e,
            }),
            DurableError::Serialization(e) => {
                ToolError::Error(SerializableToolError::Serialization {
                    message: e.to_string(),
                    source: e,
                })
            }
            DurableError::TaskNotRegistered { task_name } => {
                ToolError::Error(SerializableToolError::ToolNotFound { name: task_name })
            }
            DurableError::TaskAlreadyRegistered { task_name } => {
                ToolError::Error(SerializableToolError::DuplicateToolName { name: task_name })
            }
            DurableError::InvalidConfiguration { reason } => {
                ToolError::Error(SerializableToolError::InvalidConfiguration { reason })
            }
            DurableError::ReservedHeaderPrefix { key } => {
                ToolError::Error(SerializableToolError::ReservedHeaderPrefix { key })
            }
            DurableError::InvalidEventName { reason } => {
                ToolError::Error(SerializableToolError::InvalidEventName { reason })
            }
        }
    }
}
