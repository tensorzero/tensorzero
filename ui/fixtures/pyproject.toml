[project]
name = "fixtures"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["pyarrow"]

[tool.uv]
exclude-newer = "3 days"
constraint-dependencies = ["urllib3>=2.6.0"]
