[profile.default]
retries = { backoff = "fixed", count = 2, delay = "3s", jitter = true }
slow-timeout = { period = "15s", terminate-after = 2 }

[profile.ci]
retries = { backoff = "exponential", count = 4, delay = "5s", jitter = true, max-delay = "60s" }
