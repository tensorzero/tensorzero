[project]
name = "tensorzero-openai-python"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "tensorzero @ file://${PROJECT_ROOT}/../python-pyo3",
    "openai",
    "pydantic"
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio"
]
