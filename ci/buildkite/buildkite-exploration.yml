steps:
  - label: ":rust: Rust Unit Tests"
    command: bash ./ci/buildkite/buildkite-exploration.sh
    plugins:
      - docker:
          image: "rust"
      - test-collector#v1.0.0:
          files: "target/nextest/ci-unit/junit.xml"
          format: "junit"
