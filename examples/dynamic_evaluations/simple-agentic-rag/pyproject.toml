[project]
name = "tensorzero-examples-rag-retrieval-augmented-generation-simple-agentic-rag"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ipykernel",
    "jupyter",
    "markdownify",
    "tensorzero @ file://${PROJECT_ROOT}/../../../clients/python", # TODO: eventually we should remove this
    "wikipedia",
]
