[project]
name = "data-extraction-ner-tinker"
version = "0.1.0"
description = "Data Extraction (NER) with LoRA fine-tuning via Tinker"
requires-python = ">=3.11"
dependencies = [
    "tinker",
    "tinker-cookbook @ git+https://github.com/thinking-machines-lab/tinker-cookbook.git",
    "pandas",
    "tqdm",
    "tabulate",
]
