[project]
name = "tensorzero-examples-guides-evals"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["tensorzero"]

[tool.uv]
exclude-newer = "3 days"
exclude-newer-package = { tensorzero = false }
