[project]
name = "tensorzero-examples-docs-guides-gateway-generate-structured-outputs-json-mode"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.101.0",
]

[tool.uv]
exclude-newer = "3 days"
