{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
      "request": {
        "type": "object",
        "description": "The request object provided by the pokemon-showdown simulator"
      },
      "context": {
        "type": "object",
        "description": "Additional information about all of the current pokemon, as previously described"
      },
      "prevError": {
        "type": ["string", "null"],
        "description": "The error produced by the simulator, if the previous action fialed"
      }
    },
    "required": ["request", "context", "prevError"],
    "additionalProperties": false
  }