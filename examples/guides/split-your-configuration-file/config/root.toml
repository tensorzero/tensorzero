[models.my_gpt_5_mini]
routing = ["openai"]

[models.my_gpt_5_mini.providers.openai]
type = "openai"
model_name = "gpt-5-mini"

[gateway]
# Optional: Enable filesystem access for MiniJinja templates to use `include` and `import` directives.
# This allows templates to import content from other template files at runtime.
#
# By default, filesystem access is disabled for security reasons, since template imports
# are evaluated dynamically and could potentially access sensitive files.
#
# To enable filesystem access, set `template_filesystem_access.enabled = true`.
# When using glob patterns for configuration files, you must specify `template_filesystem_access.base_path`.
# Template imports will then be resolved relative to this base path.
template_filesystem_access = { enabled = true, base_path = "." }  # if `base_path` is relative, it'll be relative to this file
