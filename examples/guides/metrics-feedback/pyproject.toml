[project]
name = "tensorzero-examples-guides-metrics-feedback"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "openai",
    "tensorzero",
]

[tool.uv]
exclude-newer = "3 days"
exclude-newer-package = { tensorzero = false }
