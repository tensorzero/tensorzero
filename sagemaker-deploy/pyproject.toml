[project]
name = "sagemaker-deploy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["sagemaker", "boto3", "huggingface_hub", "awscli"]
