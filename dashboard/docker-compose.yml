services:
  clickhouse:
    container_name: tensorzero-clickhouse-dashboard-tests
    image: clickhouse/clickhouse-server:latest
    tmpfs:
      # So the fixtures aren't persisted between restarts of the containers and double-added
      - /var/lib/clickhouse
    ports:
      - "8123:8123" # HTTP port
      - "9000:9000" # Native port
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:8123/ping",
        ]
      start_period: 30s
      start_interval: 1s
      timeout: 1s

  gateway:
    container_name: tensorzero-gateway-dashboard-tests
    image: tensorzero/gateway
    volumes:
      - ./fixtures/config:/app/config:ro
    environment:
      - CLICKHOUSE_URL=http://tensorzero-clickhouse-dashboard-tests:8123/tensorzero
    ports:
      - "3000:3000"
    depends_on:
      clickhouse:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:3000/health",
        ]
      start_period: 1s
      start_interval: 1s
      timeout: 1s

  fixtures:
    container_name: tensorzero-fixtures-dashboard-tests
    image: clickhouse/clickhouse-server:latest
    volumes:
      - ./fixtures:/fixtures:ro
    environment:
      - CLICKHOUSE_HOST=tensorzero-clickhouse-dashboard-tests
    depends_on:
      gateway:
        condition: service_healthy
    command: ["bash", "-c", "cd /fixtures && ./load_fixtures.sh"]
