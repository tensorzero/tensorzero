[project]
name = "tensorzero-recipes-dicl"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "clickhouse-connect",
    "ipykernel",
    "openai",
    "pandas",
    "tensorzero",
    "toml",
    "tqdm",
]

[tool.uv]
constraint-dependencies = ["urllib3>=2.6.0"]
