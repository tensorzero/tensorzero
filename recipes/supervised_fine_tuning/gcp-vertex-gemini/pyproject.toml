[project]
name = "tensorzero-recipes-supervised-fine-tuning-metrics-gcp-vertex-gemini"
version = "0.0.0"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "clickhouse-connect",
    "google-cloud-aiplatform",
    "ipykernel",
    "notebook",
    "numpy",
    "pandas",
    "tensorzero",
    "toml",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
